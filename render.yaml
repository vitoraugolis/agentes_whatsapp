services:
  - type: web
    name: agentes-whatsapp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webhook_whatsapp:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
